[project]
name = "high-performance-byzantine-firewall"
authors = ["Network Security Team"]
description = "A decentralized, reputation-based network defense mechanism using blockchain technology"
telemetry = false
requirements = []
[contracts.fitnest-core]
path = "contracts/fitnest-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
